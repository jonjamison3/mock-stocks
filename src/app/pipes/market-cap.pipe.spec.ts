import { MarketCapPipe } from './market-cap.pipe';

describe('MarketCapPipe', () => {
  it('create an instance', () => {
    const pipe = new MarketCapPipe();
    expect(pipe).toBeTruthy();
  });
});
